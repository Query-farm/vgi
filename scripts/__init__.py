# Marker so `python -m scripts.stress` can import the stress package from the
# repo root. The other files in scripts/ are standalone scripts and are
# unaffected by this being importable as a package.
